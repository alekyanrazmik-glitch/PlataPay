# Local dev secrets for `wrangler dev`. Copy to `.dev.vars` (gitignored,
# never commit it) and fill in real values. In production these are set
# with `wrangler secret put <NAME>` instead — see README.md.

ENOT_SHOP_ID=
ENOT_SECRET_KEY=
ENOT_SECRET_KEY_2=
ENOT_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
